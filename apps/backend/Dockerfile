# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @developer/backend`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t @developer/backend`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system @developer/backend && \
          adduser --system -G @developer/backend @developer/backend

COPY apps/backend/dist @developer/backend/
RUN chown -R @developer/backend:@developer/backend .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix @developer/backend --omit=dev -f install

CMD [ "node", "@developer/backend" ]
